apiVersion: v1
kind: Service
metadata:
  name: lobsterpool
  namespace: lobsterpool
spec:
  selector:
    app: lobsterpool
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
